# Optional: set your Git commit identity automatically during setup.
# Copy this file to: config/user.env (no ".example") and fill it in.
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL="you@example.com"

# Optional: where your Codex workspaces should live
CODEX_WORKSPACES_DIR="$HOME/codex-workspaces"
